<%- include('../partials/head', {pageName: employee.name}) %>
<body>
    <!-- <h1>Name: <%= employee.name %></h1>
    <img src="<%= employee.image %>" alt="Ogre image">
    <h3>Role: <%= employee.role %></h3> -->
<%- include('../partials/nav', {pageName: employee.name}) %>
    <div class="container my-2 py-3 d-flex justify-content-center">
        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src=<%=employee.image %> alt=<%= employee.name %>>
                <div class="card-body">
                    <h5 class="card-title">Name: <%= employee.name %>
                    </h5>
                    <p>Role: <%= employee.role %>
                    </p>
                </div>
                <form class="text-center" method="POST" action="/ems/<%= employee._id %>?_method=DELETE">
                    <input class="btn btn-danger my-2" type="submit" value="Delete Aritst">
                </form>
                <div class="text-center">
                    <a class="btn btn-warning" href="/ems/<%= employee._id %>/edit">Update Employee</a>
                </div>
        </div>
      
    </div>
    
    
    

<%- include('../partials/footer') %>   
</body>
</html>